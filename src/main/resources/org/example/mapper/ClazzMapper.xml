<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.ClazzMapper">

    <!--查询班级信息-->
    <select id="list" resultType="org.example.pojo.Clazz"
            parameterType="org.example.pojo.ClazzQueryParam">
        SELECT c.*, e.name master_name,
        case
            when NOW() &gt; c.end_date then '已结课'
            when NOW() &lt; c.begin_date then '未开班'
            else '在读中'
        END AS status
        FROM clazz c LEFT JOIN emp e ON c.master_id=e.id

        <where>
            <!-- 班级名称 -->
            <if test="name != null and name != ''">
                AND c.name LIKE CONCAT('%', #{name}, '%')
            </if>

            <!-- 开课时间（大于等于） -->
            <if test="begin != null">
                AND c.begin_date <![CDATA[>=]]> #{begin}
            </if>

            <!-- 结课时间（小于等于） -->
            <if test="end != null">
                AND c.end_date <![CDATA[<=]]> #{end}
            </if>
        </where>

        ORDER BY c.update_time DESC
    </select>

    <!--统计班级人数-->
    <select id="studentCountData" resultType="java.util.Map">
        SELECT
            c.name as name,
            count(*) AS value
        FROM student s LEFT JOIN clazz c ON s.clazz_id=c.id GROUP BY c.update_time DESC
    </select>



    <!--修改班级信息-->
    <update id="updateClazz">
        UPDATE clazz
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="room != null">room = #{room},</if>
            <if test="beginDate != null">begin_date = #{beginDate},</if>
            <if test="endDate != null">end_date = #{endDate},</if>
            <if test="masterId != null">master_id = #{masterId},</if>
            <if test="subject != null">subject = #{subject},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>
</mapper>